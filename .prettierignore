# The specification is hand-formatted and binding (CLAUDE.md). Letting a
# formatter rewrite it would produce enormous diffs that hide real contract
# changes in the noise — and documentation drift is a defect precisely because
# it has to stay reviewable.
docs/
README.md
CLAUDE.md

# Generated / vendored
node_modules/
.venv/
pnpm-lock.yaml
uv.lock
.next/
dist/
fixtures/synthetic-repo/
# The error corpus is emitted by fixtures/corpus/generate.py, which is its
# formatter. Letting prettier reformat it too would mean the committed files no
# longer match what regenerating produces, so every regeneration would show a
# spurious diff and `make fixtures-verify` would be comparing against a file
# nothing writes.
fixtures/error-corpus/
infra/supabase/.temp/
